#include <10F220.h>
#fuses nowdt, mclr
#use delay(clock = 4 MHz)
int8 x ;
void main()
{
  output_high(pin_b1);
   while(TRUE)
   {
   /*
     if(input(pin_b0) == 1){
       while(input(pin_b0) == 1);
       delay_ms(50);
       x++;                 motoru butona  3. basmadan 5. basmaya ve 7. basmadan
       if(x == 11) x=0;     8. basmaya kadar çalýþtýrýr.
     }
     if((x >= 3 && x <= 5) || (x >= 7 && x <= 8)) {
     output_low(pin_b1);
     }
     else{
     output_high(pin_b1);
     } */
     
     
   }

}
